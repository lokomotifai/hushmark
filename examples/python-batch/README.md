# Python batch example

With the local core running, execute:

```sh
uv run python examples/python-batch/main.py --core-url http://127.0.0.1:8000
```

The output includes masked text and mapping metadata. Original values are omitted by default.
